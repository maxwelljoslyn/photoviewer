[project]
name = "photoviewer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.2.15",
    "pillow>=12.2.0",
    "reverse-geocoder>=1.5.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.18",
]
